{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Solicituts list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datasolicitut</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('user_solicituts_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dataSolicitut %}{{ entity.dataSolicitut|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('user_solicituts_show', { 'id': entity.id }) }}">Mostra</a>
                    </li>
                    <li>
                        <a href="{{ path('user_solicituts_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('user_solicituts_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
