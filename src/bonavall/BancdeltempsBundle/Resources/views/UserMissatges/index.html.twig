{% extends 'bonavallBancdeltempsBundle:Default:skel.html.twig' %}

{% block body -%}
    <h1>Missatges list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Missatge</th>
                <th>Data</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}

            <tr>
                <td><a href="{{ path('user_missatges_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.missatge }}</td>
                <td>{% if entity.data %}{{ entity.data|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('user_missatges_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('user_missatges_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('user_missatges_edit', { 'id': id }) }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
