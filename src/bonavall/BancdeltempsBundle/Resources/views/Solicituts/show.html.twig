{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Solicituts</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Datasolicitut</th>
                <td>{{ entity.dataSolicitut|date('d-m-Y') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_solicituts') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_solicituts_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
