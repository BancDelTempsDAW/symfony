{% extends 'bonavallBancdeltempsBundle:Default:skel.html.twig' %}

{% block body -%}
    <h1>Estat Servei</h1>

<div class="panel panel-default">
    <div class="container-fluid">
        <div class="table-responsive" >
    <table class="table table-condensed">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Descripcio</th>
                <td>{{ entity.descripcio }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_estatservei') }}">
            torna a la llista
        </a>
    </li>
    <li>
        <a href="{{ path('admin_estatservei_edit', { 'id': entity.id }) }}">
            Edita
        </a>
    </li>

</ul>
</div>
</div>
</div>
{% endblock %}
