{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Poblacion</h1>
<div class="panel panel-default">
    <div class="container-fluid">
        <div class="table-responsive" >
    <table class="table table-condensed">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Poblacio</th>
                <td>{{ entity.poblacio }}</td>
            </tr>
            <tr>
                <th>Codipostal</th>
                <td>{{ entity.codiPostal }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_poblacion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_poblacion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>

</ul>
</div>
</div>
</div>        
{% endblock %}
