{% extends '::base.html.twig' %}

{% block body -%}
<h1>Denuncies edit</h1>

<div class="panel panel-default">
    <div class="container-fluid">
    {{ form(edit_form) }}

            <ul class="record_actions">
                <li>
                    <a href="{{ path('admin_denuncies') }}">
                        Back to the list
                    </a>
                </li>
                <li>{{ form(delete_form) }}</li>
            </ul>
        </div>
    </div>
{% endblock %}
