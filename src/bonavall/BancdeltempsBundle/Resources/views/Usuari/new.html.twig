{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Usuari creation</h1>

    {{ form(form) }}
    {% if form.vars.value.image is not null %}
            <img src="{{ asset('upload/dir/' ~ form.vars.value.fotografia) }}" />
        {% endif %}

        <ul class="record_actions">
    <li>
        <a href="{{ path('usuari') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
