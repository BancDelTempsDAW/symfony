{% extends 'bonavallBancdeltempsBundle:Default:skel.html.twig' %}

{% block body -%}
    <h1>Banc del temps</h1>

<div class="panel panel-default">
    <div class="container-fluid">
        <div class="table-responsive" >
            <br />
            <table class="table table-condensed">
                <thead>
                <tr class="info">
                <th>Id</th>
                <th>Nom</th>
                <th>Saldo Mínim</th>
                <th>Temps d'inactivitat</th>
                <th>Accions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('Admin_bancdeltemps_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.saldoMinim }}</td>
                <td>{{ entity.tempsInactivitat }}</td>
                <td>
                    <ul style="list-style:none;">
                    <li>
                        <a href="{{ path('Admin_bancdeltemps_show', { 'id': entity.id }) }}">Veure</a>
                    </li>
                    <li>
                        <a href="{{ path('Admin_bancdeltemps_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

            <ul style="list-style:none;">
        <li>
            <a href="{{ path('Admin_bancdeltemps_new') }}">Crear nova entrada</a>
        </li>
    </ul>
            </div>
        </div>
    </div>

    {% endblock %}
