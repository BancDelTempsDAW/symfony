{% extends 'bonavallBancdeltempsBundle:Default:skel.html.twig' %}

{% block body -%}
    <h1>Bancdeltemps list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Saldominim</th>
                <th>Tempsinactivitat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('Admin_bancdeltemps_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.saldoMinim }}</td>
                <td>{{ entity.tempsInactivitat }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('Admin_bancdeltemps_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('Admin_bancdeltemps_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('Admin_bancdeltemps_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
