{% extends 'bonavallBancdeltempsBundle:Default:skel.html.twig' %}
{% block body %}
{% if error %}
<div class="bg-danger"><h3>{{ error.message }}</h3></div>
{% endif %}
<form role="form" action="{{ path('login_check') }}" method="post">
    <div class="form-group">
        <label for="username">Email:</label>
        <input id="username" type="text" name="_username" value="{{ last_username }}" placeholder="Introdueix email" class="form-control" />
    </div>
    <div class="form-group">
        <label for="password">Contraseña:</label>
        <input id="password" type="password" name="_password" placeholder="Password" class="form-control" /> 
    </div>
    <input type="submit" name="login" class="btn btn-default"/>
</form>
{% endblock %}
