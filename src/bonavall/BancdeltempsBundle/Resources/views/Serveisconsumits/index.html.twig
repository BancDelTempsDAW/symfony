{% extends 'bonavallBancdeltempsBundle:Default:skel.html.twig' %}

{% block body -%}
    <h1>Serveisconsumits list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Comentaris</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_serveisconsumits_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.comentaris }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_serveisconsumits_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_serveisconsumits_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_serveisconsumits_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
