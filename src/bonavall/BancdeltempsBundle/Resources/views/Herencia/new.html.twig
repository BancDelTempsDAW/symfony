{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Herencia creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('herencia') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
